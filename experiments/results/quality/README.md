# Kết quả chất lượng dữ liệu

- `knowledge_quality_audit.json`: audit processed CSV bởi
  `experiments.evaluation.audit_knowledge_quality`.
- `neo4j_validation.json`: validation graph do `src.runtime.prepare` ghi lại.

Hai artifact phải cùng processed snapshot trước khi dùng trong báo cáo.
