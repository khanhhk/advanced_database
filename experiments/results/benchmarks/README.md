# Kết quả benchmark

Chứa CSV latency và metadata cho Neo4j, SQLite và benchmark đa quy mô. CSV chỉ có
ý nghĩa khi đọc cùng file `.metadata.json` tương ứng.

So sánh chỉ hợp lệ trên cùng máy, snapshot, warm-up và số iteration; không dùng
kết quả để tuyên bố xếp hạng engine một cách phổ quát.
