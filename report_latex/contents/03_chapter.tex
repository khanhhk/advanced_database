% LaTeX source maintained directly for Overleaf.
\chapter{III. Thiết kế và cài đặt hệ thống}

\section{Lựa chọn, cấu hình và triển khai DBMS/công cụ}

\subsection{Kiến trúc tổng thể}
\reportfigure{system_architecture.pdf}{Kiến trúc tổng thể của hệ thống}{fig:system-architecture}

\begin{Verbatim}[fontsize=\scriptsize,frame=single]
TMDB API/cache --+
                 +-> clean/normalize/exact-ID enrich -> CSV + manifest
IMDb ratings.gz -+                          +-> Neo4j -> QA/recommend -> FastAPI/UI
                                            +-> RDF -> entailment -> SPARQL
\end{Verbatim}

QA dùng parser chín intent, entity linker và query catalog có tham số. Neo4j là
nguồn dữ liệu và bằng chứng của runtime.

\subsection{Yêu cầu phi chức năng}

Hệ thống phải idempotent, query-safe, có provenance, chạy demo không phụ thuộc
Internet sau import, không commit API key/raw dataset, và có workflow tái lập.
Public API là read-only; CRUD chỉ phục vụ maintenance có kiểm soát. Câu hỏi ngoài
chín intent phải được từ chối rõ ràng và không ảnh hưởng recommendation.

\section{Chuẩn bị bộ dữ liệu thực nghiệm}

\subsection{Thu thập TMDB}

Collector duyệt popular pages đến khi đủ số movie ID duy nhất, sau đó tải detail,
credits, keywords và external IDs theo TMDB API \cite{tmdb2026api}. Response được cache nguyên dạng; request có
timeout, retry và interval. Cache giúp chạy lại không tiêu tốn API quota và giữ
snapshot nguồn cho reproducibility.

\subsection{Tích hợp IMDb}

Pipeline chỉ tải \texttt{title.ratings.tsv.gz} từ bộ dữ liệu phi thương mại IMDb \cite{imdb2026datasets}, kiểm tra gzip/checksum và ghi metadata.
File được stream từng dòng, chỉ giữ record có \texttt{tconst} thuộc tập IMDb ID của 5.000
phim TMDB. Cách này không giải nén toàn bộ và không nạp IMDb đầy đủ vào RAM/Neo4j.
\texttt{Movie.rating} và \texttt{Movie.imdb\_rating} không ghi đè nhau.

\subsection{Quản trị nguồn, điều khoản sử dụng và đạo đức dữ liệu}

TMDB API được sử dụng theo tài liệu và điều khoản dành cho nhà phát triển; khóa
API là secret cục bộ, không đưa vào repository. Dữ liệu hiển thị phải ghi nhận
TMDB theo yêu cầu của nền tảng và không được mô tả như dữ liệu do nhóm sở hữu \cite{tmdb2026api}.
IMDb cung cấp các dataset này cho mục đích sử dụng phi thương mại; đề tài chỉ tải
file ratings công khai cần thiết, không phân phối lại toàn bộ dataset và không
mở rộng phạm vi sang mục đích thương mại \cite{imdb2026datasets}. Người tái lập phải tự kiểm tra điều
khoản hiện hành tại thời điểm chạy vì điều khoản dịch vụ có thể thay đổi.

Corpus chỉ chứa metadata nghề nghiệp công khai về phim và người tham gia, không
thu thập lịch sử xem, hồ sơ người dùng hoặc dữ liệu nhạy cảm. Rating của nguồn là
tín hiệu tổng hợp, không được diễn giải như sự thật khách quan về chất lượng phim.
Sampling theo popular pages có popularity bias; giới hạn cast tạo coverage bias.
Hai bias này được công bố để tránh suy rộng kết quả sang toàn bộ điện ảnh hoặc
mọi nhóm người dùng.

\subsection{Cleaning và entity resolution}
\reportfigure{entity_resolution_flow.pdf}{Quy trình cleaning và phân giải thực thể}{fig:entity-resolution}

Cleaning chuẩn hóa Unicode/whitespace, kiểu số/ngày và trường bắt buộc. Duplicate
TMDB ID bị loại và log. Exact TMDB/IMDb ID được ưu tiên; fuzzy name fallback dùng
threshold và từ chối tied top score. Ambiguous case không được tự động merge.

\subsection{Normalized artifacts và manifest}

Pipeline tạo \texttt{movies.csv}, \texttt{people.csv}, \texttt{genres.csv}, \texttt{keywords.csv},
\texttt{studios.csv} và năm edge CSV. Manifest ghi source checksum, thời điểm tạo, counts,
IMDb coverage/match, invalid record, duplicate/missing rate và coverage cast,
director, genre. Raw, interim và processed data được tách để tránh nhầm nguồn với
artifact dẫn xuất.

\subsection{Thuật toán pipeline}
\reportfigure{etl_pipeline.pdf}{Luồng xử lý dữ liệu từ nguồn đến các graph}{fig:etl-pipeline}

\begin{Verbatim}[fontsize=\scriptsize,frame=single]
INPUT: raw TMDB records, optional compressed IMDb ratings
1. Validate required movie ID/title; reject and log invalid records.
2. Deduplicate Movie by tmdb_id.
3. Normalize scalar fields and names.
4. Build stable IDs from TMDB source IDs.
5. Emit node maps and relationship rows with source metadata.
6. Stream IMDb ratings; retain only wanted tconst values.
7. Enrich Movie without overwriting TMDB rating.
8. Write deterministic CSV tables.
9. Compute checksum, counts, error rates and coverage into manifest.
OUTPUT: normalized CSVs + manifest + invalid-record log
\end{Verbatim}

Độ phức tạp bộ nhớ của IMDb join phụ thuộc số IMDb ID thuộc tập phim mục tiêu,
không phụ thuộc toàn bộ file ratings. Entity maps phụ thuộc số entity trong
2.000–5.000 phim. Cast limit kiểm soát kích thước nhưng đồng thời tạo coverage bias.

\subsection{Xử lý lỗi}

Downloader dùng file \texttt{.part}, chỉ rename sau khi gzip hợp lệ để tránh coi partial
download là hoàn chỉnh. Collector retry lỗi tạm thời và cache response. Pipeline
fail rõ nếu IMDb path được chỉ định nhưng không tồn tại. Runtime không tự dùng seed
khi thiếu data thật; nó dừng và yêu cầu chuẩn bị dataset. Cách fail closed tránh
demo vô tình chạy fixture rồi báo như corpus thật.

\subsection{Tái lập và thay đổi nguồn}

TMDB/IMDb là nguồn động, nên chạy lại ngày khác có thể tạo snapshot khác.
Reproducibility ở đây là tái lập workflow và truy vết snapshot, không cam kết API
luôn trả byte giống nhau. Raw cache/checksum cho phép chạy transform trên snapshot
cũ. Nếu data thay đổi, evaluation/benchmark phải chạy lại cùng snapshot.

\subsection{Idempotency và reproducibility}

Idempotency nghĩa là thực hiện cùng một import nhiều lần tạo cùng trạng thái logic,
không nhân đôi node/edge. Reproducibility rộng hơn: người khác phải biết input,
version, configuration, code path và protocol để tái tạo artifact/kết quả. \texttt{MERGE}
giải quyết một phần idempotency; checksum, manifest, metadata máy và command
workflow giải quyết reproducibility. Import có thể idempotent nhưng không tái lập
nếu nguồn thay đổi mà không có snapshot/checksum.

\section{Thiết kế và tinh chỉnh ontology hoặc lược đồ Knowledge Graph}

\subsection{Các lớp và định danh}
\reportfigure{ontology_diagram.pdf}{Sơ đồ lớp của ontology miền phim}{fig:ontology-diagram}

\begin{longtable}{|>{\raggedright\arraybackslash}p{0.29\textwidth}|>{\raggedright\arraybackslash}p{0.29\textwidth}|>{\raggedright\arraybackslash}p{0.29\textwidth}|}
\caption{Tổng hợp các lớp và định danh}\label{tab:3-1} \\
\hline
\textbf{Class/label} & \textbf{Khóa} & \textbf{Thuộc tính chính} \\ \hline
\endfirsthead\hline \textbf{Class/label} & \textbf{Khóa} & \textbf{Thuộc tính chính} \\ \hline\endhead
Movie & \texttt{tmdb\_id} & imdb\_id, title, release\_date, runtime, rating, imdb\_rating, imdb\_votes, popularity, overview \\ \hline
Person & \texttt{person\_id} & tmdb\_id, imdb\_id, name, source \\ \hline
Genre & \texttt{genre\_id} & name, source \\ \hline
Keyword & \texttt{keyword\_id} & name, source \\ \hline
Studio & \texttt{company\_id} & name, country, source \\ \hline
\end{longtable}

Person dùng một label duy nhất vì một người có thể vừa đóng phim vừa đạo diễn.
Vai trò được biểu diễn bởi relationship. Với dữ liệu mới, \texttt{person\_id=tmdb:<id>};
hash tên chỉ là legacy fallback khi fixture/cache cũ không có source ID.

\subsection{Relationship}
\reportfigure{property_graph_schema.pdf}{Lược đồ node và relationship trong Neo4j}{fig:property-graph-schema}

\begin{longtable}{|>{\raggedright\arraybackslash}p{0.29\textwidth}|>{\raggedright\arraybackslash}p{0.29\textwidth}|>{\raggedright\arraybackslash}p{0.29\textwidth}|}
\caption{Tổng hợp relationship}\label{tab:3-2} \\
\hline
\textbf{Relationship} & \textbf{Hướng} & \textbf{Property} \\ \hline
\endfirsthead\hline \textbf{Relationship} & \textbf{Hướng} & \textbf{Property} \\ \hline\endhead
ACTED\_IN & Person → Movie & character, cast\_order, source \\ \hline
DIRECTED & Person → Movie & source \\ \hline
HAS\_GENRE & Movie → Genre & source \\ \hline
HAS\_KEYWORD & Movie → Keyword & source \\ \hline
PRODUCED\_BY & Movie → Studio & source \\ \hline
CO\_STARRED\_WITH & Person → Person & movie\_count, evidence\_movie\_ids, derived \\ \hline
\end{longtable}

\subsection{Ontology RDF/OWL}

Ontology khai báo năm class disjoint, object property với domain/range, inverse
\texttt{actedIn/hasActor}, inverse \texttt{directed/directedBy}, symmetric \texttt{coStarredWith}, và
functional \texttt{tmdbId/imdbId}. Datatype property mô tả title, ratings, votes và name.
Functional property không đồng nghĩa global inverse-functional identity; stable
URI vẫn được tạo từ source ID.

\subsection{Ánh xạ RDF–Neo4j}

\begin{longtable}{|>{\raggedright\arraybackslash}p{0.44\textwidth}|>{\raggedright\arraybackslash}p{0.44\textwidth}|}
\caption{Tổng hợp ánh xạ rdf–neo4j}\label{tab:3-3} \\
\hline
\textbf{Neo4j} & \textbf{RDF/OWL} \\ \hline
\endfirsthead\hline \textbf{Neo4j} & \textbf{RDF/OWL} \\ \hline\endhead
\texttt{(:Movie \{tmdb\_id\})} & \texttt{:movie/<id> a :Movie; :tmdbId ...} \\ \hline
\texttt{(:Person)-[:ACTED\_IN]->(:Movie)} & \texttt{:person/... :actedIn :movie/...} \\ \hline
\texttt{[:DIRECTED]} & \texttt{:directed} / inverse \texttt{:directedBy} \\ \hline
edge property & cần reification/RDF-star nếu xuất đầy đủ \\ \hline
constraint & ontology axiom + validation đóng \\ \hline
\end{longtable}

\subsection{Các quyết định mô hình hóa}

\subsubsection{Một Person label thay vì Actor/Director}

Nếu dùng hai label loại trừ nhau, người vừa diễn vừa đạo diễn phải duplicate node
hoặc mang hai label không phản ánh role theo từng phim. Một \texttt{Person} và role trên
relationship tránh hai vấn đề này. Query “phim của một người” có thể match
\texttt{ACTED\_IN|DIRECTED}, còn query chuyên biệt vẫn chọn đúng type.

\subsubsection{Source ID thay cho name}

Tên không ổn định và không duy nhất. Source ID được dùng làm constraint key; name
là thuộc tính hiển thị/tìm kiếm. Fuzzy matching chỉ liên kết input người dùng tới
entity đã có, không thay stable identity trong storage.

\subsubsection{Tách TMDB và IMDb rating}

Hai rating có population và thời điểm cập nhật khác nhau. Gộp vào một property sẽ
mất provenance. Vì vậy \texttt{rating}, \texttt{imdb\_rating}, \texttt{imdb\_votes} được giữ riêng. Khi
QA cần một rating ưu tiên, query catalog dùng \texttt{coalesce(imdb\_rating,rating)} theo quyết
định công khai.

\subsubsection{Materialize co-star}

Co-star có thể tính tại query time nhưng được dùng thường xuyên và cần audit.
Materialization lưu aggregate/evidence và minh họa derived fact. Chi phí là phải
recompute sau authoritative import; runtime preparation xử lý phụ thuộc này.

\subsection{Từ điển dữ liệu và quy tắc miền}

\begin{longtable}{|>{\raggedright\arraybackslash}p{0.23\textwidth}|>{\raggedright\arraybackslash}p{0.14\textwidth}|>{\raggedright\arraybackslash}p{0.09\textwidth}|>{\raggedright\arraybackslash}p{0.14\textwidth}|>{\raggedright\arraybackslash}p{0.22\textwidth}|}
\caption{Tổng hợp từ điển dữ liệu và quy tắc miền}\label{tab:3-4} \\
\hline
\textbf{Trường} & \textbf{Kiểu/miền} & \textbf{Bắt buộc} & \textbf{Nguồn} & \textbf{Quy tắc và\linebreak{}ý nghĩa} \\ \hline
\endfirsthead\hline \textbf{Trường} & \textbf{Kiểu/miền} & \textbf{Bắt buộc} & \textbf{Nguồn} & \textbf{Quy tắc và\linebreak{}ý nghĩa} \\ \hline\endhead
Movie.tmdb\_id & integer dương & có & TMDB & khóa ổn định, duy nhất trong Movie \\ \hline
Movie.imdb\_id & chuỗi \texttt{tt...} & không & TMDB external ID & khóa nối chính xác sang IMDb \\ \hline
Movie.title & Unicode string & có & TMDB & trim whitespace, không được rỗng \\ \hline
\texttt{Movie.}\linebreak{}\texttt{release\_date} & ISO date & không & TMDB & ngày phát hành, không phải valid-time của temporal KG \\ \hline
Movie.rating & số 0–10 & không & TMDB & giữ riêng với IMDb rating \\ \hline
\texttt{Movie.}\linebreak{}\texttt{imdb\_rating} & số 0–10 & không & IMDb & chỉ ghi khi exact \texttt{imdb\_id} match \\ \hline
\texttt{Movie.}\linebreak{}\texttt{imdb\_votes} & integer không âm & không & IMDb & số phiếu tại snapshot thu thập \\ \hline
\texttt{Person.}\linebreak{}\texttt{person\_id} & source-qualified ID & có & TMDB/\linebreak{}pipeline & identity key, không dùng tên làm khóa \\ \hline
\texttt{ACTED\_IN.}\linebreak{}\texttt{character} & string & không & TMDB credits & vai diễn được công bố ở nguồn \\ \hline
\texttt{ACTED\_IN.}\linebreak{}\texttt{cast\_order} & integer không âm & không & TMDB credits & thứ tự cast, không phải độ quan trọng tuyệt đối \\ \hline
\texttt{CO\_STARRED\_WITH.}\linebreak{}\texttt{movie\_count} & integer dương & có & luật & số phim chung hỗ trợ fact suy ra \\ \hline
\texttt{CO\_STARRED\_WITH.}\linebreak{}\texttt{evidence\_}\linebreak{}\texttt{movie\_ids} & danh sách ID & có & luật & đường truy vết về các Movie tiền đề \\ \hline
\end{longtable}

Các miền giá trị được kiểm tra ở pipeline/validation thay vì xem OWL là cơ chế
kiểm tra dữ liệu đóng. Required field là yêu cầu của ứng dụng; functional và
disjoint axiom là yêu cầu ngữ nghĩa; constraint Neo4j bảo vệ identity/storage.
Ba lớp ràng buộc bổ sung cho nhau nhưng không được đánh đồng.

\section{Cài đặt trên Knowledge Graph engine và cấu hình reasoner}

Neo4j là operational Knowledge Graph engine. RDFLib cung cấp nhánh kiểm thử semantic
nhẹ; Apache Jena Fuseki 6.1.0 là triple-store/SPARQL endpoint độc lập dùng để chạy
cùng tập luật suy diễn trên full RDF snapshot.

\subsection{Import và validation Neo4j}

Thứ tự import là constraint/index → node → edge → reasoning → validation. Mỗi
batch dùng parameterized \texttt{UNWIND}; \texttt{MERGE} trên stable key giúp import lặp không
tạo duplicate. \texttt{--replace} dành cho authoritative rebuild. Validation kiểm tra:

\begin{itemize}
\item orphan Movie;
\item duplicate stable IDs;
\item missing required property;
\item invalid relationship type;
\item endpoint/hướng sai schema;
\item \texttt{CO\_STARRED\_WITH} không còn supporting movie.
\end{itemize}

Integration test dùng Neo4j test riêng ở cổng 7688 với storage tạm, import hai lần
và kiểm tra QA, recommendation, CRUD; graph demo không bị reset.

Jena chạy trong Docker profile \texttt{semantic}, đọc ontology và \texttt{movies.ttl} qua
assembler. \texttt{GenericRuleReasoner} ở chế độ forward nạp năm luật khai báo tường minh:
domain, range, hai chiều của \texttt{owl:inverseOf} và \texttt{owl:SymmetricProperty}. Profile
này cố ý khớp phạm vi mà báo cáo tuyên bố, tránh ngụ ý hỗ trợ toàn bộ OWL 2 DL.
Fuseki chỉ phục vụ endpoint query cho workflow đánh giá, không nằm trên critical
path của \texttt{make demo}.
